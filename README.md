# Notas
Este es mi primer programa en code

```
Options:
      --help     Show help          [boolean]
      --version  Show version number[boolean]
  -b, --base     Es la base de la tabla de
                 multiplicar
                          [number] [required]
  -l, --listar   Muestra la tabla en consolta
                   [boolean] [default: false]
  -h, --hasta    Muestra la tabla en consolta
                       [number] [default: 10]
```
